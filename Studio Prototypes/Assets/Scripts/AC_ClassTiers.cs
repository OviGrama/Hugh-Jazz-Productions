using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class AC_ClassTiers : MonoBehaviour
{

    // These hold the bool variables so that they can be used to check the appropriate value.
    // What tier is the class.
    public bool tier1Class;
    public bool tier2Class;
    public bool tier3Class;
    // What type of class is it.
    public bool iqClass;
    public bool fitnessClass;
    public bool superClass;

    // Start is called before the first frame update
    void Start()
    {

    }
}